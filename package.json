{
  "name": "loom-cross-reality-persistence-layer",
  "version": "0.1.0",
  "description": "A persistence layer for cross-reality applications, enabling seamless state synchronization between physical and digital realms.",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cross-reality",
    "persistence",
    "metaverse",
    "digital-twin",
    "loom"
  ],
  "author": "Bahattin Yunus",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
